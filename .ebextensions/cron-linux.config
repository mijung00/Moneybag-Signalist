files:
  "/etc/cron.d/my_cron_job":
    # ... (생략)
    content: |
      # 예: 10분 뒤 실행 (UTC 시간 계산 조심! 한국시간 -9시간)
      # 지금 한국 00:00 -> UTC 15:00
      # 테스트 시간: UTC 15:15 (한국 00:15) 정도로 설정 추천
      13 16 * * * root bash /var/app/current/run_moneybag.sh

      # (빈 줄 필수)