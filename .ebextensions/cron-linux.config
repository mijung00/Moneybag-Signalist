files:
  "/etc/cron.d/my_cron_job":
    mode: "000644"
    owner: root
    group: root
    content: |
      # KST 19:55 (UTC 10:55)에 실행하도록 변경
      55 10 * * * root set -a; . /opt/elasticbeanstalk/deployment/env; set +a; cd /var/app/current && /var/app/venv/*/bin/python -m iceage.src.pipelines.daily_collector >> /var/log/web.stdout.log 2>&1
      
      # 🚨 중요: 이 줄 뒤에 엔터키를 한 번 더 눌러서 완전히 빈 줄을 만드세요. 🚨


    

  




      
commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/my_cron_job.bak"
