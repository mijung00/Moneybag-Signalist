files:
  "/etc/cron.d/my_cron_job":
    mode: "000644"
    owner: root
    group: root
    content: |
      # KST 22:30 (UTC 13:30) 실행
      # 환경 변수 로드 방식을 'source'로 유지 (안전함)
      25 13 * * * root source /opt/elasticbeanstalk/deployment/env && cd /var/app/current && /var/app/venv/*/bin/python3.14 -m iceage.src.pipelines.daily_runner >> /var/log/web.stdout.log 2>&1
      
      # 🚨 여기 빈 줄(엔터) 필수!
      
commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/my_cron_job.bak"