name: Deploy to AWS Elastic Beanstalk

on:
  push:
    branches:
      - main  # 'main' 브랜치에 푸시될 때만 실행

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. 깃허브에 있는 코드 내려받기 (데이터 파일 없음)
    - name: Checkout source code
      uses: actions/checkout@v3

    # 2. 배포용 압축파일 만들기 (git 관련 파일 제외)
    - name: Generate deployment package
      run: zip -r deploy.zip . -x "*.git*" "*.github*" "venv/*" "__pycache__/*" "*.DS_Store"

    # 3. AWS Elastic Beanstalk으로 배포 쏘기 🚀
    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v21
      with:
        aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        application_name: Moneybag-Signalist      # ⚠️ AWS 콘솔에 있는 '애플리케이션 이름' 확인 후 수정!
        environment_name: Moneybag-Signalist-env-2  # ⚠️ AWS 콘솔에 있는 '환경 이름' 확인 후 수정!
        version_label: ${{ github.sha }}          # 버전 라벨 (커밋 해시값 사용)
        region: ap-northeast-2                    # 서울 리전
        deployment_package: deploy.zip